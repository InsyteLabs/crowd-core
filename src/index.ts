'use strict';

import { UserService } from './services';
import { User } from './models';
const db = require('./db');

const userService = new UserService();

(async () => {

    try{
             
    }
    catch(e){
        console.log(e);
    }

})();